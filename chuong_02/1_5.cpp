#include<iostream>
using namespace std;
int main()
{
    int m1 = 0,n1  = 0;
    int temp = 0;
    while(temp<=m*n)
    {
        for(int i = 0;i<n1;i++)
        {
            a[m1][i] = b[temp++];
        }
        m1++;
    }
}